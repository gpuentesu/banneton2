import { describe, test, expect } from 'vitest';
import { createRecipe, addComponent, removeComponent } from '../lib/recipeLogic';

describe('Recipe Logic functions - RF_03', () => {
    
    describe('1. createRecipe', () => {
    
        // Aquí adentro irán nuestros bloques de pruebas
        test('should do something specific', () => {
            // Arrange
            // Act
            // Assert
        });
    });

    describe('2. addComponent', () => {
    
        // Aquí adentro irán nuestros bloques de pruebas
        test('should do something specific', () => {
            // Arrange
            // Act
            // Assert
        });
    });

    describe('3. removeComponent', () => {
    
    // Aquí adentro irán nuestros bloques de pruebas
        test('should do something specific', () => {
            // Arrange
            // Act
            // Assert
        });
    });

});